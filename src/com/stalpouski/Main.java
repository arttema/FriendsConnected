package com.stalpouski;

/**
 * Social network connectivity. Given a social network containing N members
 * and a log file containing M timestamps at which times pairs of members formed
 * friendships, design an algorithm to determine the earliest time at which all
 * members are connected (i.e., every member is a friend of a friend of a friend
 * ... of a friend). Assume that the log file is sorted by timestamp and that
 * friendship is an equivalence relation. The running time of your algorithm
 * should be MlogN or better and use extra space proportional to N.
 */
public class Main {
    int[] friends = new int[100];
    Connection[] connections = new Connection[1000];
    BinaryDu connections
    public static void main(String[] args) {




    }

    public void whenEverybodyFriend(){
        int reqConnections = friends.length;
        int relationshipCount = 0;

        for (Connection connection : connections) {
            int first = connection.friend1Id;
            int second = connection.friend2Id;

            if[id]
        }

    }
}
